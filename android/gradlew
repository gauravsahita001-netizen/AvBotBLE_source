#!/bin/sh
# Gradle wrapper start-up script (bootstrap).
# The real wrapper + gradle-wrapper.jar are generated by running:
#   cd android && gradle wrapper --gradle-version 8.11.1
# or by copying them from a fresh `npx react-native init` project.
echo "Gradle wrapper not generated yet. See README.md -> 'Gradle wrapper'." >&2
exit 1
